import Mathlib.Data.Prod.Lex

/-!
# Synonymous Algebra for Lexographic Product

Gives `α ×ₗ β` synonymous algebras to `α × β`.
That is,
Defines instances for algebraic structures up to `IdemCommSemiring` on `α ×ₗ β`
as exactly those structures on `α × β`.

 -/
