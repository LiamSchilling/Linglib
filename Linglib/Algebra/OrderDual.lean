import Mathlib.Order.Basic

/-!
# Synonymous Algebra for Order Dual

Gives `OrderDual α` synonymous algebras to `α`.
That is,
Defines instances for algebraic structures up to `IdemCommSemiring` on `OrderDual α`
as exactly those structures on `α`.

 -/
